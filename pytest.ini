[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
# Browser defaults for pytest-playwright (M1). Override on CLI if needed:
#   pytest --browser chromium --headed
